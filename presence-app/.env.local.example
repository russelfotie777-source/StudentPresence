# Adresse de l'API. Vide : l'app passe par son propre domaine (/api relayé
# vers l'API locale, voir next.config.ts) — un seul port à exposer pour une
# démonstration. En production, l'adresse publique de l'API.
NEXT_PUBLIC_API_URL=
